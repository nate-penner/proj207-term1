<!--
Page shown to a newly registered customer
Author: Nate Penner
When: December 2021
-->
<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('meta') %>
    <title>Travel Experts<%= pageTitle %></title>
    <%- include('stylesheets') %>
    <style>
      #main-content {
        width: 75%;
        margin: 0 auto;
      }
      #main-content h1 {
        text-align: center;
        font-size: 1.5em;
        font-weight: bold;
        margin-bottom: 1em;
      }
      #main-content h2 {
        text-align: center;
        font-size: 1.25em;
        font-weight: bold;
        margin-bottom: 1em;
      }
      .confirmation p {
        font-style: italic;
        margin-top: 1em;
        line-height: 1.5;
      }
      .confirmation {
        display: grid;
        margin: 0 auto;
        width: 30%;
      }
      .confirmation a {
        color: #4d4d4d;
        font-weight: bold;
      }
    </style>
    <script>
      const storage = window.sessionStorage;

      <% for (let key in details) { %>
      storage.setItem('<%= key %>', '<%= details[key] %>');
      <% } %>
    </script>
  </head>
  <body>
  <%- include('header') %>

  <section id="main-content">
    <article class="confirmation">
      <h1>Thanks for registering, <%= details.CustFirstName %>!</h1>
      <p style="font-weight: bold">Your Customer ID is <%= details.CustomerUUID %></p>
      <p>Would you like to <a href="/vacations">Book a vacation package?</a> Or view your <a href="/profile/<%= details.CustomerUUID %>">profile</a> page? Your Customer ID has been emailed to you, but to be safe, save it in a file or write it down for future use!</p>
    </article>
  </section>

  <%- include('footer') %>
  </body>
</html>